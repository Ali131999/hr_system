import 'package:flutter/material.dart';

class LeaveFormTab extends StatelessWidget {
  const LeaveFormTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: []);
  }
}
